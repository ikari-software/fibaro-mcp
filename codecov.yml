coverage:
  status:
    project:
      default:
        informational: true
    patch:
      default:
        informational: false
        target: 80%

comment:
  layout: "reach,diff,flags,tree"
  behavior: default

ignore:
  - "dist/**"
  - "**/*.test.ts"
